const getDbPool = require('db-pool');
var dbPool = getDbPool.get();

let userModel = (function(){
    function insertUserSignupData(userSignupData) {
        
    }

    return {
        insertUserSignupData
    }
}());



